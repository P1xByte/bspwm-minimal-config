#!/bin/sh

super + {_,shift +}Return
	{alacritty,bspc node -s biggest.local}

super + d
	rofi -show drun

super + Escape
	pkill -USR1 -x sxhkd

super + shift + {q,r}
	bspc {quit,wm -r}

super + shift + c
	bspc node -c

super + {_,shift + }{1-9}
	bspc {desktop -f,node -d} '^{1-9}'
